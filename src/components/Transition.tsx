import React from 'react'

const Transition = (): React.ReactElement => {
  return (
    <div
      className="w-[90vw] h-[15vh]"
      style={{
        background:
          'linear-gradient(180deg, rgba(255, 255, 255, .95) 0%, #1a1a1a 100%)',
      }}
    ></div>
  )
}

export default Transition
